# =============================================================================
# TradingAgents-CN 用户认证系统配置
# =============================================================================

# =============================================================================
# 登录要求配置
# =============================================================================

# 是否要求登录才能使用股票分析功能
# true = 需要登录 (默认，推荐)
# false = 无需登录 (开放模式)
REQUIRE_LOGIN_FOR_ANALYSIS=true

# =============================================================================
# MongoDB用户认证数据库配置
# =============================================================================

# MongoDB连接配置
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DATABASE=tradingagents
MONGODB_AUTH_SOURCE=admin

# MongoDB认证信息（建议修改默认密码）
MONGODB_USERNAME=admin
MONGODB_PASSWORD=Trade123456

# 或使用完整连接字符串（优先级更高）
# MONGODB_CONNECTION_STRING=mongodb://admin:Trade123456@localhost:27017/admin

# =============================================================================
# 默认管理员账户配置
# =============================================================================

# 默认管理员账户（系统启动时自动创建）
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=Trade123456

# =============================================================================
# 会话和安全配置
# =============================================================================

# 会话过期时间（小时）
SESSION_EXPIRE_HOURS=24

# 最小密码长度
MIN_PASSWORD_LENGTH=6

# =============================================================================
# 使用说明
# =============================================================================

# 1. 将此文件复制为 .env
# 2. 根据需要修改配置值
# 3. 重启Web应用使配置生效

# 快速启动命令：
# python -m streamlit run web/app.py

# 默认登录地址：
# http://localhost:8501

# 默认管理员登录：
# 用户名: admin
# 密码: Trade123456